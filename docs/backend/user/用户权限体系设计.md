# 用户权限体系设计

> 文档位置：`docs/backend/user/用户权限体系设计.md`
> 适用范围：`QSDMS_DESKTOP` 单租户后台权限体系
> 当前结论：采用单租户 `RBAC`，不做多租户设计。

## 1. 设计目标

当前系统是 `Flutter Desktop + GetX` 前端和 `NestJS + PostgreSQL + Prisma` 后端的桌面优先企业数据管理系统。权限体系的目标是建立一套可以支撑登录、菜单、按钮、接口和后续审计的基础能力，同时避免过早引入复杂组织权限和多租户隔离。

本设计覆盖以下内容：

- 用户账号与用户资料分离。
- 一个用户可以绑定多个角色。
- 一个角色可以绑定多个权限。
- 权限分为菜单权限、操作权限、接口权限三类。
- 前端根据权限做菜单和按钮显隐。
- 后端通过 `Guard` 和权限服务做接口强校验。
- 登录后下发最小可用的用户信息、角色和权限集合。
- 所有公共 API、DTO、数据库 schema 变更进入实现阶段前需要再次确认。

不做以下内容：

- 不做多租户，不设计 `Tenant`、`tenantId`、租户成员、租户管理员。
- 不做每个用户单独配置权限的复杂模型，首版只通过角色授权。
- 不做行级数据权限和字段级权限，后续业务需要时再单独设计。
- 不做无限层级菜单，前端侧边栏首版最多两级菜单。
- 不把前端权限显隐当作安全边界，安全边界始终在后端。

## 2. 当前项目现状

只读追踪得到的当前状态如下：

| 范围 | 当前状态 | 对本设计的影响 |
|---|---|---|
| 后端框架 | `backend/` 已有 `NestJS`、`ConfigModule`、`PrismaModule`、`OpenAPI`、全局 `/api` 前缀和 `ValidationPipe` | 权限模块可以按标准 NestJS 模块接入 |
| Prisma schema | `backend/prisma/schema.prisma` 目前只有 `generator` 和 `datasource` | RBAC 表结构属于新增 schema，实施前必须再次确认 |
| 响应结构 | `ApiResponse<T>` 目前包含 `code`、`message`、`data` | 登录、用户信息、菜单接口应沿用该结构 |
| OpenAPI | `backend/src/openapi.ts` 已配置 `BearerAuth` | 鉴权接口和权限接口应进入 OpenAPI 契约 |
| 前端 HTTP | `frontend/lib/shared/services/api_client.dart` 已有统一 `Dio` 入口 | 后续可在这里统一注入 `Authorization` header |
| 前端路由 | `AppRoutes` 已有 `/`、`/base-data`、`/settings` | 菜单权限编码应与路由保持稳定映射 |
| 前端菜单 | `SidebarMenuItemConfig` 已预留 `permissionCode` | 可直接作为前端菜单过滤和禁用依据 |
| 侧边栏文档 | 现有设计要求 `Sidebar` 只消费结构化菜单数据，不直接计算复杂权限 | 菜单过滤应放在 `AuthController/AuthState` 或上层服务 |

因此，当前项目适合采用“后端权限真相源 + 前端消费权限快照”的方案。

## 3. 总体方案

推荐方案是标准单租户 `RBAC`：

```text
User
  -> UserRole
  -> Role
  -> RolePermission
  -> Permission
```

权限按用途分类：

| 类型 | 示例编码 | 用途 | 强制边界 |
|---|---|---|---|
| `MENU` | `menu:dashboard` | 控制菜单是否展示 | 前端体验层，后端菜单接口负责过滤 |
| `ACTION` | `system:user:create` | 控制按钮、表单操作、批量动作 | 前端显隐 + 后端业务接口校验 |
| `API` | `api:users:create` | 控制具体后端接口访问 | 后端 `Guard` 强校验 |

核心原则：

- 菜单权限解决“看不看得到入口”。
- 操作权限解决“页面里能不能点某个动作”。
- API 权限解决“请求到后端是否允许执行”。
- 同一个业务动作应同时具备稳定的 `ACTION` 权限和 `API` 权限。
- 前端权限不足时隐藏或禁用入口，后端权限不足时返回 `FORBIDDEN`。

## 4. 角色模型

首版建议内置以下角色：

| 角色编码 | 中文名称 | 定位 |
|---|---|---|
| `SUPER_ADMIN` | 超级管理员 | 拥有全部系统能力，只保留给系统维护者 |
| `ADMIN` | 管理员 | 管理业务配置、普通用户和主要数据 |
| `OPERATOR` | 操作员 | 处理日常业务数据 |
| `VIEWER` | 只读用户 | 只允许查看授权范围内的数据 |

角色设计要求：

- 角色编码使用英文大写下划线。
- 角色名称面向用户显示，使用中文。
- 角色是否启用由 `isActive` 控制，禁用角色后用户不能通过该角色获得权限。
- `SUPER_ADMIN` 是系统保底角色，但实现时仍建议通过权限集合计算，而不是到处写死分支。

## 5. 权限编码规范

权限编码必须稳定、可读、可检索，不使用中文和展示文案作为权限主键。

### 5.1 菜单权限

格式：

```text
menu:<menuKey>
```

建议首批菜单权限：

| 菜单 | 路由 | 权限编码 |
|---|---|---|
| 工作台 | `/` | `menu:dashboard` |
| 基础数据 | `/base-data` | `menu:base-data` |
| 系统设置 | `/settings` | `menu:settings` |
| 用户管理 | `/settings/users` | `menu:settings-users` |
| 角色管理 | `/settings/roles` | `menu:settings-roles` |
| 权限管理 | `/settings/permissions` | `menu:settings-permissions` |

### 5.2 操作权限

格式：

```text
<domain>:<resource>:<action>
```

示例：

| 场景 | 权限编码 |
|---|---|
| 查看用户列表 | `system:user:list` |
| 创建用户 | `system:user:create` |
| 编辑用户 | `system:user:update` |
| 禁用用户 | `system:user:disable` |
| 重置密码 | `system:user:reset-password` |
| 分配角色 | `system:user:assign-roles` |
| 查看角色 | `system:role:list` |
| 编辑角色权限 | `system:role:assign-permissions` |

### 5.3 API 权限

格式：

```text
api:<resource>:<action>
```

示例：

| 接口 | 权限编码 |
|---|---|
| `GET /api/users` | `api:users:list` |
| `POST /api/users` | `api:users:create` |
| `PATCH /api/users/:id` | `api:users:update` |
| `PATCH /api/users/:id/status` | `api:users:update-status` |
| `POST /api/users/:id/reset-password` | `api:users:reset-password` |
| `PUT /api/users/:id/roles` | `api:users:assign-roles` |
| `GET /api/roles` | `api:roles:list` |
| `PUT /api/roles/:id/permissions` | `api:roles:assign-permissions` |

## 6. 数据库设计建议

后台权限类表首版建议使用 `Int` 自增主键。原因是用户、角色、权限数据规模有限，`Int` 对 `Prisma`、`JSON` 和前端模型更直接。业务单据编号、订单号、供应商编号等仍应使用独立业务编号字段，不直接暴露数据库主键作为业务编号。

### 6.1 users

账号主表，只保存登录和账号状态相关字段。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | `Int` | 主键 |
| `username` | `String` | 登录账号，唯一 |
| `passwordHash` | `String` | 密码哈希，不返回前端 |
| `status` | `UserStatus` | `ACTIVE`、`DISABLED`、`LOCKED` |
| `tokenVersion` | `Int` | 用于强制旧 Token 失效 |
| `lastLoginAt` | `DateTime?` | 最近登录时间 |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

索引：

- `users.username` 唯一。
- `users.status` 普通索引，便于后台筛选。

### 6.2 user_profiles

用户资料表，与账号一对一。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | `Int` | 主键 |
| `userId` | `Int` | 关联 `users.id`，唯一 |
| `displayName` | `String` | 用户显示名 |
| `phone` | `String?` | 手机号，可唯一 |
| `email` | `String?` | 邮箱，可唯一 |
| `avatarUrl` | `String?` | 头像地址 |
| `departmentName` | `String?` | 首版可先用文本，后续组织架构再独立建表 |
| `postName` | `String?` | 岗位名称 |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

### 6.3 roles

角色表。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | `Int` | 主键 |
| `code` | `String` | 角色编码，唯一 |
| `name` | `String` | 中文名称 |
| `description` | `String?` | 说明 |
| `isSystem` | `Boolean` | 系统内置角色，避免被误删 |
| `isActive` | `Boolean` | 是否启用 |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

### 6.4 user_roles

用户角色关系表，一个用户可以绑定多个角色。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `userId` | `Int` | 关联用户 |
| `roleId` | `Int` | 关联角色 |
| `assignedAt` | `DateTime` | 授权时间 |
| `assignedBy` | `Int?` | 授权操作人 |

约束：

- 组合主键：`[userId, roleId]`。
- 删除用户时删除关系。
- 删除系统内置角色应被业务层禁止。

### 6.5 permissions

权限定义表。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | `Int` | 主键 |
| `code` | `String` | 权限编码，唯一 |
| `name` | `String` | 中文名称 |
| `type` | `PermissionType` | `MENU`、`ACTION`、`API` |
| `module` | `String` | 模块，例如 `system`、`base-data` |
| `description` | `String?` | 说明 |
| `sortOrder` | `Int` | 展示排序 |
| `isSystem` | `Boolean` | 系统内置权限 |
| `isActive` | `Boolean` | 是否启用 |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

### 6.6 role_permissions

角色权限关系表。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `roleId` | `Int` | 关联角色 |
| `permissionId` | `Int` | 关联权限 |
| `assignedAt` | `DateTime` | 授权时间 |
| `assignedBy` | `Int?` | 授权操作人 |

约束：

- 组合主键：`[roleId, permissionId]`。
- 只允许绑定 `isActive = true` 的权限。

### 6.7 refresh_sessions

用于 refresh token 管理和安全审计。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | `String` | 会话主键，建议 `cuid()` |
| `userId` | `Int` | 关联用户 |
| `refreshTokenHash` | `String` | refresh token 哈希 |
| `userAgent` | `String?` | 客户端信息 |
| `ipAddress` | `String?` | 登录 IP |
| `expiresAt` | `DateTime` | 过期时间 |
| `revokedAt` | `DateTime?` | 撤销时间 |
| `createdAt` | `DateTime` | 创建时间 |

### 6.8 audit_logs

后台安全相关操作建议记录审计日志。

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | `Int` | 主键 |
| `actorUserId` | `Int?` | 操作人 |
| `action` | `String` | 操作类型 |
| `resourceType` | `String` | 资源类型 |
| `resourceId` | `String?` | 资源 ID |
| `metadata` | `Json?` | 脱敏后的附加信息 |
| `ipAddress` | `String?` | IP |
| `userAgent` | `String?` | 客户端信息 |
| `createdAt` | `DateTime` | 创建时间 |

审计日志禁止保存明文密码、Token、数据库连接串和其他敏感配置。

## 7. 后端模块设计

建议目录：

```text
backend/src/
  modules/
    auth/
      auth.controller.ts
      auth.module.ts
      auth.service.ts
      decorators/
        api-permission.decorator.ts
        current-user.decorator.ts
        public.decorator.ts
      dto/
      guards/
        jwt-auth.guard.ts
        api-permission.guard.ts
      strategies/
        jwt.strategy.ts
    users/
      users.controller.ts
      users.module.ts
      users.service.ts
      dto/
    roles/
      roles.controller.ts
      roles.module.ts
      roles.service.ts
      dto/
    permissions/
      permissions.controller.ts
      permissions.module.ts
      permissions.service.ts
    menus/
      menus.controller.ts
      menus.module.ts
      menus.service.ts
```

职责边界：

| 模块 | 职责 |
|---|---|
| `auth` | 登录、刷新 Token、退出登录、当前用户、JWT Guard、权限 Guard |
| `users` | 用户列表、创建用户、编辑资料、禁用用户、重置密码、分配角色 |
| `roles` | 角色列表、角色创建、角色编辑、角色权限分配 |
| `permissions` | 权限定义查询、权限初始化、权限树输出 |
| `menus` | 根据当前用户权限返回可见菜单 |

## 8. 认证与授权流程

### 8.1 登录流程

```text
POST /api/auth/login
  -> 校验 username/password
  -> 校验用户 status
  -> 查询用户角色和权限
  -> 写入 refresh_sessions
  -> 返回 accessToken、refreshToken、user、roles、permissions、menus
```

登录响应建议：

```ts
type LoginResponse = {
  accessToken: string;
  refreshToken: string;
  user: {
    id: number;
    username: string;
    displayName: string;
    avatarUrl?: string;
  };
  roles: Array<{
    code: string;
    name: string;
  }>;
  permissions: string[];
  menus: MenuNode[];
};
```

注意：

- `passwordHash`、`refreshTokenHash`、`tokenVersion` 不返回前端。
- `permissions` 返回权限编码列表，前端用 `Set<String>` 缓存。
- `menus` 由后端按权限过滤后返回，前端不自己拼完整菜单树。

### 8.2 Access Token

`accessToken` 建议包含最小必要信息：

```ts
type JwtPayload = {
  sub: number;
  username: string;
  tokenVersion: number;
};
```

不建议把全部权限编码直接塞进 `accessToken`，原因是：

- 权限列表可能增长。
- 角色权限变更后 Token 内权限会过期。
- 后端接口校验需要以当前数据库权限为准，至少应具备可刷新机制。

首版可以在登录时返回权限快照给前端用于 UI，后端 `Guard` 查询缓存或数据库完成强校验。

### 8.3 接口授权流程

接口使用装饰器声明所需权限：

```ts
@ApiPermission('api:users:create')
@Post()
createUser() {}
```

请求进入后：

```text
JwtAuthGuard
  -> 解析 accessToken
  -> 校验用户存在、状态正常、tokenVersion 有效
  -> ApiPermissionGuard
  -> 读取接口元数据中的权限编码
  -> 校验当前用户角色是否拥有该 API 权限
  -> 放行或返回 FORBIDDEN
```

建议错误码：

| 场景 | HTTP 状态 | `code` |
|---|---|---|
| 未登录 | `401` | `UNAUTHORIZED` |
| Token 过期 | `401` | `TOKEN_EXPIRED` |
| 用户禁用 | `403` | `USER_DISABLED` |
| 权限不足 | `403` | `FORBIDDEN` |
| 角色禁用 | `403` | `ROLE_DISABLED` |

## 9. 菜单与前端权限接入

前端已有 `SidebarMenuItemConfig.permissionCode`，后续应保持该边界：

- `Sidebar` 只渲染传入的菜单数据。
- `Sidebar` 不直接请求权限接口。
- `Sidebar` 不直接计算复杂权限。
- 登录态、权限集合和菜单数据由上层 `AuthController` 或 `ShellController` 管理。

推荐前端目录：

```text
frontend/lib/
  modules/
    auth/
      auth_controller.dart
      auth_repository.dart
      login_page.dart
      models/
    system/
      users/
      roles/
      permissions/
  shared/
    services/
      api_client.dart
      token_storage.dart
```

推荐前端权限判断：

```dart
class AuthState {
  AuthState({required this.permissions});

  final Set<String> permissions;

  bool can(String code) => permissions.contains(code);
}
```

菜单处理：

```text
后端返回 menus
  -> AuthController 保存菜单树
  -> AppShell 将菜单树转换为 SidebarMenuItemConfig
  -> Sidebar 渲染
```

按钮处理：

```text
页面 Controller 持有 AuthState
  -> can('system:user:create')
  -> 控制按钮显示或禁用
```

API 请求处理：

- `ApiClient` 统一添加 `Authorization: Bearer <accessToken>`。
- 收到 `401 TOKEN_EXPIRED` 时尝试刷新 Token。
- 收到 `403 FORBIDDEN` 时提示“无权限执行此操作”，不要重复重试。

## 10. 初始接口清单

### 10.1 Auth

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| `POST` | `/api/auth/login` | 登录 | public |
| `POST` | `/api/auth/refresh` | 刷新 Token | refresh token |
| `POST` | `/api/auth/logout` | 退出登录 | authenticated |
| `GET` | `/api/auth/me` | 当前用户信息 | authenticated |
| `GET` | `/api/auth/permissions` | 当前用户权限 | authenticated |

### 10.2 Menus

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| `GET` | `/api/menus/me` | 当前用户可见菜单 | authenticated |

### 10.3 Users

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| `GET` | `/api/users` | 用户列表 | `api:users:list` |
| `POST` | `/api/users` | 创建用户 | `api:users:create` |
| `GET` | `/api/users/:id` | 用户详情 | `api:users:detail` |
| `PATCH` | `/api/users/:id` | 编辑用户 | `api:users:update` |
| `PATCH` | `/api/users/:id/status` | 启用、禁用、锁定用户 | `api:users:update-status` |
| `POST` | `/api/users/:id/reset-password` | 重置密码 | `api:users:reset-password` |
| `PUT` | `/api/users/:id/roles` | 分配角色 | `api:users:assign-roles` |

### 10.4 Roles

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| `GET` | `/api/roles` | 角色列表 | `api:roles:list` |
| `POST` | `/api/roles` | 创建角色 | `api:roles:create` |
| `PATCH` | `/api/roles/:id` | 编辑角色 | `api:roles:update` |
| `PUT` | `/api/roles/:id/permissions` | 分配角色权限 | `api:roles:assign-permissions` |

### 10.5 Permissions

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| `GET` | `/api/permissions` | 权限列表 | `api:permissions:list` |
| `GET` | `/api/permissions/tree` | 权限树 | `api:permissions:list` |

## 11. 种子数据策略

首版应提供固定 seed：

- 创建 `SUPER_ADMIN`、`ADMIN`、`OPERATOR`、`VIEWER` 四个角色。
- 创建首批菜单、操作、API 权限。
- 给 `SUPER_ADMIN` 绑定所有权限。
- 创建一个本地开发管理员账号。
- 管理员默认密码只允许在本地 seed 文档或 `.env.example` 中说明，不写入生产配置。

建议脚本：

```text
backend/prisma/seed.ts
```

或：

```text
backend/src/scripts/seed-default-permissions.ts
```

要求：

- seed 必须幂等。
- 权限编码变更要显式处理旧权限，不静默制造重复数据。
- 生产环境不得自动重置管理员密码。

## 12. 安全规则

必须遵守：

- 密码使用 `argon2` 或 `bcrypt` 哈希，不保存明文密码。
- refresh token 只保存哈希。
- 日志中不输出密码、Token、数据库连接串、用户隐私数据。
- 禁用用户后应立即使旧 Token 失效。
- 重置密码应递增 `tokenVersion`，让旧 Token 失效。
- 编辑角色权限后，后端权限缓存必须失效。
- 删除系统内置角色和系统内置权限应被禁止。
- `SUPER_ADMIN` 至少保留一个有效用户，避免系统被锁死。

## 13. 验收标准

### 13.1 需求验收

使用 EARS 描述核心验收条件：

1. 当用户提交正确账号密码且账号状态为 `ACTIVE` 时，系统应返回登录 Token、用户资料、角色列表、权限编码集合和可见菜单。
2. 当用户账号状态为 `DISABLED` 或 `LOCKED` 时，系统应拒绝登录并返回稳定错误码。
3. 当用户访问未授权 API 时，系统应返回 `403 FORBIDDEN`，并且不得执行目标业务动作。
4. 当角色权限被调整后，系统应确保后续接口校验使用最新权限。
5. 当用户没有某个菜单权限时，系统应不向前端返回该菜单节点。
6. 当前端收到 `FORBIDDEN` 时，系统应显示无权限提示，不应继续重试同一请求。
7. 当管理员重置用户密码或禁用用户时，系统应使该用户旧 Token 失效。
8. 当系统内置角色或权限被删除时，系统应拒绝该操作。

### 13.2 技术验收

- `backend/prisma/schema.prisma` 中存在用户、角色、权限和关系表。
- 所有新增 DTO 使用 `class-validator` 校验。
- 所有受保护接口通过 `JwtAuthGuard`。
- 所有需要权限的接口声明 `@ApiPermission(...)`。
- 前端 `ApiClient` 统一注入 Token。
- 前端菜单来自后端当前用户菜单接口，不再使用静态默认值作为真实菜单。
- OpenAPI 能导出登录、用户、角色、权限接口。

## 14. 测试策略

后端测试：

| 测试类型 | 覆盖点 |
|---|---|
| `AuthService` 单元测试 | 密码校验、用户状态、Token 生成、refresh session |
| `ApiPermissionGuard` 单元测试 | 无权限、权限匹配、角色禁用、用户禁用 |
| `UsersService` 单元测试 | 创建用户、分配角色、禁用用户、重置密码 |
| `RolesService` 单元测试 | 角色创建、权限分配、系统角色保护 |
| e2e 测试 | 登录、获取菜单、无权限 API 返回 `403` |

前端测试：

| 测试类型 | 覆盖点 |
|---|---|
| `AuthController` 测试 | 登录成功、登录失败、权限集合保存 |
| `ApiClient` 测试 | Token 注入、`401` 刷新、`403` 提示 |
| 菜单测试 | 无权限菜单不展示，有权限菜单展示 |
| 页面测试 | 无权限按钮隐藏或禁用 |

建议验证命令：

```bash
cd backend
pnpm prisma:generate
pnpm test
pnpm test:e2e
pnpm build
```

```bash
cd frontend
flutter test
flutter analyze
```

## 15. 实施阶段建议

### 阶段 1：后端 schema 与 seed

- 新增用户、角色、权限、关系表。
- 新增状态枚举和权限类型枚举。
- 编写幂等 seed。
- 验证 `prisma generate` 和基础测试。

### 阶段 2：认证模块

- 接入密码哈希库。
- 实现登录、刷新、退出、当前用户接口。
- 实现 `JwtAuthGuard` 和 `CurrentUser`。
- 补充 auth 单元测试和 e2e 测试。

### 阶段 3：权限模块

- 实现 `@ApiPermission()` 装饰器。
- 实现 `ApiPermissionGuard`。
- 实现权限查询和权限树接口。
- 给用户、角色接口补权限校验。

### 阶段 4：菜单与前端接入

- 后端实现 `/api/menus/me`。
- 前端新增 `AuthController`、`AuthRepository`、`TokenStorage`。
- `ApiClient` 注入 Token。
- `AppShell` 使用后端菜单替换静态默认菜单。
- 使用 `permissionCode` 控制按钮和菜单状态。

### 阶段 5：管理页面

- 用户管理页面。
- 角色管理页面。
- 权限树选择组件。
- 重置密码、禁用用户、分配角色等操作。

## 16. 后续实现前必须确认的事项

进入编码前，需要单独确认以下变更：

- 是否采用本文建议的 `Int` 自增主键。
- 是否一次性引入 `refresh_sessions` 和 `audit_logs`。
- 是否首版内置 `SUPER_ADMIN`、`ADMIN`、`OPERATOR`、`VIEWER` 四个角色。
- 是否首版实现系统设置下的用户、角色、权限管理页面。
- 初始开发管理员账号的用户名和本地默认密码规则。
- 是否允许登录接口直接返回菜单树，还是登录后再请求 `/api/menus/me`。
